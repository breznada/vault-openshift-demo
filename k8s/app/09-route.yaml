---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: vault-demo-app
  namespace: vault-demo
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  labels:
    app: vault-demo-app
spec:
  # Let OpenShift generate the hostname automatically
  # It will be: vault-demo-app-vault-demo.apps.<cluster-domain>
  to:
    kind: Service
    name: vault-demo-app
    weight: 100
  port:
    targetPort: http
  tls:
    # Card 4: Use certificate from VaultPKISecret
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
    # Reference the TLS secret created by VaultPKISecret
    externalCertificate:
      name: vault-pki-cert
  wildcardPolicy: None

# Made with Bob