---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-secrets
  namespace: openshift-gitops
spec:
  project: default
  
  sources:
    - repoURL: https://github.com/breznada/vault-openshift-demo.git
      targetRevision: main
      path: k8s/vault-crds
  
  destination:
    server: https://kubernetes.default.svc
    namespace: vault-demo
  
  # Ignore differences in operator-managed fields
  ignoreDifferences:
    - group: secrets.hashicorp.com
      kind: VaultDynamicSecret
      jsonPointers:
        - /status
    - group: secrets.hashicorp.com
      kind: VaultStaticSecret
      jsonPointers:
        - /status
    - group: secrets.hashicorp.com
      kind: VaultPKISecret
      jsonPointers:
        - /status
    - group: secrets.hashicorp.com
      kind: CSISecrets
      jsonPointers:
        - /status
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

# Made with Bob
